import reducer from './reducers'

import * as feedSelectors from './selectors'
import feedSagas from './sagas'
import * as feedTypes from './types'

export { feedSelectors, feedSagas, feedTypes }

export default reducer
